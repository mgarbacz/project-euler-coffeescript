// Generated by CoffeeScript 1.4.0
(function() {
  var isPalindrome, solution;

  isPalindrome = function(num) {
    var divider, left, right;
    if (num < 0) {
      return false;
    }
    divider = 1;
    while ((num / divider) >= 10) {
      divider *= 10;
    }
    while (num !== 0) {
      left = Math.floor(num / divider);
      right = num % 10;
      if (left !== right) {
        return false;
      }
      num = Math.floor((num % divider) / 10);
      divider /= 100;
    }
    return true;
  };

  solution = function() {
    var largestPalindrome, num1, num2, product, _i, _j;
    largestPalindrome = 0;
    for (num1 = _i = 999; _i >= 0; num1 = _i += -1) {
      for (num2 = _j = 999; _j >= 0; num2 = _j += -1) {
        product = num1 * num2;
        if (isPalindrome(product) && product > largestPalindrome) {
          largestPalindrome = product;
        }
      }
    }
    return largestPalindrome;
  };

  console.log(solution());

}).call(this);
